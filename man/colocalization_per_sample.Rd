% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_spatial.R
\name{colocalization_per_sample}
\alias{colocalization_per_sample}
\title{Per-sample segmentation-free co-localization of gene sets}
\usage{
colocalization_per_sample(
  tx,
  sample_key,
  group_key,
  patient_key = NULL,
  gene_sets,
  pairs = NULL,
  r = c(10, 25, 50),
  method = c("empirical", "model"),
  bin_size = 4,
  relative = TRUE,
  model_type = c("composition", "relative", "full"),
  n_cores = 1,
  ...
)
}
\arguments{
\item{tx}{Transcript table with a sample column.}

\item{sample_key, group_key, patient_key}{Column names.}

\item{gene_sets}{Named list of gene vectors (e.g. cell-type markers).}

\item{pairs}{Two-column data.frame or list of length-2 character vectors
of gene-set names to test (default: all unordered pairs, including
self-pairs).}

\item{r}{Distances at which to report `log_g`.}

\item{method}{`"empirical"` or `"model"`.}

\item{bin_size}{Bin size passed to [bin_transcripts()].}

\item{relative}{For `method = "empirical"`: if `TRUE` (default), report
the relative pair correlation (label-permutation O/E; see
[pcf_cross()]), which is robust to differences in overall cellularity
between samples.}

\item{model_type}{For `method = "model"`: the `type` passed to
[rff_pair_correlation()] (default `"composition"`).}

\item{n_cores}{Number of samples processed in parallel (forked
processes via [parallel::mclapply()]; sequential on Windows).}

\item{...}{Passed to [fit_spatial_rff()] (for `method = "model"`) or
[bin_transcripts()].}
}
\value{
A data.frame with `sample_id`, `cluster_i`, `cluster_j`, `r`,
  `log_g`, `n_transcripts`, `group`, `patient`.
}
\description{
**Experimental.** For every sample, bin the
transcripts and compute the log cross pair correlation between gene sets
at the requested distances, either model-free ([pcf_cross()]) or from a
fitted random-feature LGCP ([fit_spatial_rff()] +
[rff_pair_correlation()]). The tidy output goes straight into
[compare_groups()] with `value = "log_g"` and
`pair_keys = c("cluster_i", "cluster_j", "r")`.
}
\examples{
tx <- simulate_transcripts_groups(n_samples_per_group = 2, size = 120,
                                  rate = 0.01)
sets <- split(unique(tx[, c("gene", "gene_set")])$gene,
              unique(tx[, c("gene", "gene_set")])$gene_set)
res <- colocalization_per_sample(tx, "sample_id", "group", "patient",
                                 gene_sets = sets,
                                 pairs = list(c("A", "B")), r = c(10, 20))
res
}
