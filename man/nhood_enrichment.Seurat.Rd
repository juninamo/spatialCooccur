% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohalu_functions.R
\name{nhood_enrichment.Seurat}
\alias{nhood_enrichment.Seurat}
\title{Neighborhood Enrichment (Seurat Method)}
\usage{
nhood_enrichment.Seurat(
  seurat_obj,
  cluster_key,
  neighbors.k = 30,
  connectivity_key = "nn",
  transformation = TRUE,
  n_perms = 100,
  seed = 1938493,
  n_jobs = 4
)
}
\arguments{
\item{seurat_obj}{A Seurat object with spatial coordinates.}

\item{cluster_key}{Metadata column for cluster IDs.}

\item{neighbors.k}{Number of neighbors to construct graph.}

\item{connectivity_key}{Which graph to use: "nn" or "snn".}

\item{transformation}{If `TRUE` (default), each link from cell u is
weighted 1 / (1 + d_u), with d_u the number of cells that chose u as a
neighbour: a mild down-weighting of hub cells. With a kNN graph every
cell sends k links at any density, so this is not a density correction;
density is handled by the label shuffles.}

\item{n_perms}{Number of permutations for significance testing.}

\item{seed}{Random seed for reproducibility.}

\item{n_jobs}{Number of cores to use in parallel.}
}
\value{
Updated Seurat object; `misc[[paste0(cluster_key, "_nhood_enrichment")]]`
  holds `zscore`, `count`, `expected` (permutation mean), `log2_oe`,
  `log2_oe_raw`, `pvalue`, `padj` and `padj_bh` (see [nhood_enrichment()]).
}
\description{
Neighborhood Enrichment (Seurat Method)
}
\examples{
df <- generate_sim(close_ratio = 0.8, n_types = 4, n_cells = 300,
                   max_loc = 300, test_type = "distribute",
                   distance_param = 10, seed = 1)
df$sample_id <- "fov1"
seu <- sim_to_seurat(df)
seu <- nhood_enrichment.Seurat(seu, cluster_key = "cell_type",
                               neighbors.k = 10, n_perms = 20, n_jobs = 1)
res <- SeuratObject::Misc(seu, slot = "cell_type_nhood_enrichment")
round(res$zscore, 1)
}
