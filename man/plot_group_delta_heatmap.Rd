% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_compare_plots.R
\name{plot_group_delta_heatmap}
\alias{plot_group_delta_heatmap}
\title{Heatmap of the disease-group effect for every cluster pair}
\usage{
plot_group_delta_heatmap(
  compare_df,
  value = "effect",
  significance = "padj",
  sig_threshold = 0.05,
  pair_keys = c("cluster_i", "cluster_j"),
  palette = c("RdBu", "viridis"),
  limits = NULL
)
}
\arguments{
\item{compare_df}{Output of [compare_groups()].}

\item{value}{Column to map to fill. Defaults to "effect".}

\item{significance}{Column name carrying p-values to overlay as labels
(set to NULL to skip).}

\item{sig_threshold}{Adjusted p-value threshold for the asterisk marker.}

\item{pair_keys}{Two column names identifying the cluster pair on the
x and y axes. Defaults to `c("cluster_i", "cluster_j")`.}

\item{palette}{One of "RdBu" (diverging, default) or "viridis".}

\item{limits}{Optional length-2 numeric vector for the fill scale limits.
If NULL, symmetric limits around 0 are used for diverging palettes.}
}
\value{
A ggplot object.
}
\description{
Plot `effect` (or another column from a [compare_groups()] result) as a
cluster_i x cluster_j heatmap, with optional significance markers.
}
