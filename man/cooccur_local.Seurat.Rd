% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohalu_functions.R
\name{cooccur_local.Seurat}
\alias{cooccur_local.Seurat}
\title{Local Co-occurrence Score (Seurat Method)}
\usage{
cooccur_local.Seurat(
  seurat_obj,
  cluster_x,
  cluster_y,
  connectivity_key = "nn",
  cluster_key = "seurat_clusters",
  sample_key = "sample_id",
  neighbors.k = 20,
  radius = 30,
  maxnsteps = 15
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{cluster_x}{First cluster of interest.}

\item{cluster_y}{Second cluster of interest.}

\item{connectivity_key}{Graph type to use.}

\item{cluster_key}{Metadata column with cluster info.}

\item{sample_key}{Metadata column with sample ID.}

\item{neighbors.k}{Number of neighbors to build graph.}

\item{radius}{Radius for proximity-based interaction.}

\item{maxnsteps}{Maximum number of diffusion steps (each step starts from
the previous one; early stop on the kurtosis criterion, see
[cooccur_local()]). `0` returns the raw indicator.}
}
\value{
A data.frame with local co-occurrence scores.
}
\description{
Local Co-occurrence Score (Seurat Method)
}
\examples{
df <- generate_sim(close_ratio = 0.8, n_types = 4, n_cells = 300,
                   max_loc = 300, test_type = "distribute",
                   distance_param = 10, seed = 1)
df$sample_id <- "fov1"
seu <- sim_to_seurat(df)
sc <- cooccur_local.Seurat(seu, cluster_x = "cell_type_1",
                           cluster_y = "cell_type_2",
                           cluster_key = "cell_type", sample_key = "sample_id",
                           neighbors.k = 10, radius = 20, maxnsteps = 1)
summary(sc[[1]])
}
