% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_spatial.R
\name{simulate_transcripts}
\alias{simulate_transcripts}
\title{Simulate transcript coordinates from a multivariate log-Gaussian Cox process}
\usage{
simulate_transcripts(
  gene_sets = c("A", "B", "C"),
  n_genes_per_set = 5,
  size = 500,
  resolution = 2,
  rate = 0.02,
  density_sd = 0.5,
  density_lengthscale = 80,
  set_sd = 1,
  set_lengthscale = 15,
  coloc = c(A = 0.8, B = 0.8),
  niche_lengthscale = 20,
  seed = 1
)
}
\arguments{
\item{gene_sets}{Character vector of gene-set names.}

\item{n_genes_per_set}{Number of genes in each set.}

\item{size}{Side length of the square window (micrometres).}

\item{resolution}{Simulation grid spacing (micrometres).}

\item{rate}{Baseline transcripts per square micrometre per gene.}

\item{density_sd, density_lengthscale}{SD and length scale of the shared
cellularity field.}

\item{set_sd, set_lengthscale}{SD and length scale of the set-specific fields.}

\item{coloc}{Named numeric vector of niche loadings, e.g.
`c(A = 0.8, B = 0.8)` makes sets A and B co-localize; opposite signs make
them segregate.}

\item{niche_lengthscale}{Length scale of the niche field.}

\item{seed}{Random seed.}
}
\value{
A data.frame with columns `x`, `y`, `gene`, `gene_set`, carrying an
  attribute `truth` (the generating parameters) usable with
  [lgcp_true_pair_correlation()].
}
\description{
**Experimental.** Genes are organized in gene
sets (e.g. marker genes of cell types). The log-intensity of gene `j` in
set `S` at location `u` is
\deqn{\log\lambda_j(u) = \log\rho + \sigma_d d(u) + \sigma_s f_S(u) + c_S n(u),}
where `d` is a shared cellularity field, `f_S` a set-specific territory
field and `n` a "niche" field shared by the sets in `coloc`. All fields are
unit-variance Gaussian random fields with an RBF covariance, generated
exactly by FFT (not with random features). Transcripts are Poisson counts
on a `resolution` grid, placed uniformly within each grid cell.
}
\examples{
tx <- simulate_transcripts(size = 150, rate = 0.01, coloc = c(A = 1, B = 1))
head(tx)
table(tx$gene_set)
}
