% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_compare.R
\name{cooccur_local_per_sample}
\alias{cooccur_local_per_sample}
\title{Per-sample local co-occurrence summary}
\usage{
cooccur_local_per_sample(
  obj,
  sample_key,
  group_key,
  cluster_key,
  cluster_x,
  cluster_y,
  patient_key = NULL,
  unit = c("image", "patient"),
  neighbors.k = 20,
  radius = 30,
  maxnsteps = 1,
  summarize = c("mean", "q90", "pos_rate")
)
}
\arguments{
\item{obj}{A Seurat object, a list of Seurat objects, or a data.frame.}

\item{sample_key, group_key, cluster_key, patient_key}{Sample / group /
cluster / patient column names. See [build_sample_design()].}

\item{cluster_x}{First cluster of interest.}

\item{cluster_y}{Second cluster of interest.}

\item{unit}{"image" or "patient" (averaged across images of the same
patient).}

\item{neighbors.k}{Number of neighbors.}

\item{radius}{Radius for neighborhood.}

\item{maxnsteps}{Number of diffusion steps.}

\item{summarize}{Character vector of summary statistics to compute:
"mean", "q90" (90th percentile), and / or "pos_rate" (fraction of
cells with score > 0).}
}
\value{
A data.frame with one row per sample carrying the requested
  summary statistics.
}
\description{
Run [cooccur_local()] for one (cluster_x, cluster_y) pair on each sample
and summarize the per-cell scores at the sample level. Output is a tidy
data.frame suitable for group comparison.
}
