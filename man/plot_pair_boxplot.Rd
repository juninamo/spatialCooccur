% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_compare_plots.R
\name{plot_pair_boxplot}
\alias{plot_pair_boxplot}
\title{Per-sample boxplot of one (or several) cluster pair(s) across groups}
\usage{
plot_pair_boxplot(
  per_sample_df,
  value = "zscore",
  group_key = "group",
  pair_keys = c("cluster_i", "cluster_j"),
  pairs = NULL,
  point = TRUE,
  add_p = FALSE,
  ref_group = NULL
)
}
\arguments{
\item{per_sample_df}{Tidy per-sample data.frame from a `*_per_sample()`
helper.}

\item{value}{Numeric column to plot. Defaults to "zscore".}

\item{group_key}{Column with disease group. Defaults to "group".}

\item{pair_keys}{Column names identifying the cluster pair.}

\item{pairs}{Optional data.frame (or list of length-2 vectors) selecting
which cluster pairs to plot. If NULL, plots all pairs as a facetted
grid (use sparingly for many pairs).}

\item{point}{Logical, overlay individual sample points.}

\item{add_p}{Logical, overlay a Wilcoxon p-value on each panel.}

\item{ref_group}{Optional ref group for the displayed p-value sign /
ordering. Passed to [compare_groups()] when computing p-values.}
}
\value{
A ggplot object.
}
\description{
Per-sample boxplot of one (or several) cluster pair(s) across groups
}
