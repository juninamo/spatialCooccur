% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_compare.R
\name{nhood_enrichment_per_sample}
\alias{nhood_enrichment_per_sample}
\title{Per-sample neighborhood enrichment}
\usage{
nhood_enrichment_per_sample(
  obj,
  sample_key,
  group_key,
  cluster_key,
  patient_key = NULL,
  unit = c("image", "patient"),
  cluster_levels = NULL,
  neighbors.k = 30,
  connectivity_key = "nn",
  transformation = TRUE,
  n_perms = 100,
  seed = 1938493,
  n_jobs = 1
)
}
\arguments{
\item{obj}{A Seurat object, a list of Seurat objects, or a data.frame
with x, y, cluster_key, and sample_key columns.}

\item{sample_key}{Column / image identifier defining a sample.}

\item{group_key}{Column carrying the disease / condition label.}

\item{cluster_key}{Column with cluster labels.}

\item{patient_key}{Optional column with patient ID (random effect /
permutation block).}

\item{unit}{"image" returns one row per image; "patient" averages across
images within a patient (requires `patient_key`).}

\item{cluster_levels}{Optional character vector of cluster levels to use
as common dimnames across samples. Defaults to the union across all
samples.}

\item{neighbors.k}{Number of neighbors to use.}

\item{connectivity_key}{Type of graph: "nn" or "snn".}

\item{transformation}{Whether to normalize adjacency matrix.}

\item{n_perms}{Number of permutations.}

\item{seed}{Random seed.}

\item{n_jobs}{Number of parallel jobs.}
}
\value{
A data.frame (also tagged with class `spatialCooccurSample`) with
  columns `sample_id`, `cluster_i`, `cluster_j`, `zscore`, `count`,
  `group`, `patient`.
}
\description{
Run [nhood_enrichment()] independently for each sample (image) in the
input, returning a tidy long data.frame with one row per
`sample_id x cluster_i x cluster_j`. The cluster factor levels are
harmonized across samples so the resulting table is suitable for
downstream group comparison with [compare_groups()].
}
